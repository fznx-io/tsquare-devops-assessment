apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsquare-apis.fullname" . }}
  labels:
    {{- include "tsquare-apis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsquare-apis.selectorLabels" . | nindent 4 }}
