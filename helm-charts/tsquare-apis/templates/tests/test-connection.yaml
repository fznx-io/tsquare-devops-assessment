apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsquare-apis.fullname" . }}-test-connection"
  labels:
    {{- include "tsquare-apis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsquare-apis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
